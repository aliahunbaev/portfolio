import FooterLab from "../components/footer-lab";

// PROTOTYPE — nothing links here. Three footer candidates on one page:
// keys 1/2/3 (or the switcher) swap variants, R toggles the MORE-style
// wipe reveal. The winner gets extracted into the layout; this route
// and footer-lab.tsx then disappear.
export default function Footers() {
  return <FooterLab />;
}
