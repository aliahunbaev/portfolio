import SketchGrid from "../components/sketch-grid";

export const metadata = { title: "Sketches — Ali Ahunbáev" };

export default function Sketches() {
  return (
    <main className="px-gutter pb-24 pt-30">
      <SketchGrid />
    </main>
  );
}
